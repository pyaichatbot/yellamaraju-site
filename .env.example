# Newsletter
# Set this in production to enable newsletter signup forms.
PUBLIC_BUTTONDOWN_USERNAME=your-buttondown-username

# Newsletter automation (server-side only; do not expose in client)
BUTTONDOWN_API_KEY=your-buttondown-api-key
NEWSLETTER_AUTOMATION_SECRET=replace-with-random-secret
