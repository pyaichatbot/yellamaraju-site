---
import BaseLayout from '../../layouts/BaseLayout.astro';
---

<BaseLayout title="Utilities" description="Utilities for quick AI planning, sizing, and cost checks.">
  <div class="utilities-page">
    <div class="container">
      <header class="utilities-header">
        <h1>Utilities</h1>
        <p>Token counters and calculators are on the way.</p>
      </header>

      <div class="utilities-placeholder">
        <h2>Coming soon</h2>
        <p>Working on a set of practical tools for estimating token usage, cost, and latency.</p>
        <a href="/contact" class="utilities-cta">Suggest a utility</a>
      </div>
    </div>
  </div>
</BaseLayout>

<style>
  .utilities-page {
    padding: var(--space-lg) 0 var(--space-xl);
  }

  .utilities-header {
    margin-bottom: var(--space-lg);
  }

  .utilities-header h1 {
    font-size: 2.5rem;
    margin-bottom: var(--space-sm);
  }

  .utilities-header p {
    color: var(--color-text-secondary);
    margin: 0;
  }

  .utilities-placeholder {
    border: 1px dashed var(--color-border);
    border-radius: 12px;
    padding: var(--space-xl);
    text-align: left;
    background: var(--color-surface);
  }

  .utilities-placeholder h2 {
    margin-top: 0;
  }

  .utilities-placeholder p {
    color: var(--color-text-secondary);
  }

  .utilities-cta {
    display: inline-block;
    margin-top: var(--space-md);
    color: white;
    background: var(--color-accent);
    padding: 0.65rem 1.5rem;
    border-radius: 6px;
    text-decoration: none;
    font-weight: 600;
  }

  .utilities-cta:hover {
    background: var(--color-accent-hover);
  }

  @media (max-width: 768px) {
    .utilities-header h1 {
      font-size: 2rem;
    }
  }
</style>
