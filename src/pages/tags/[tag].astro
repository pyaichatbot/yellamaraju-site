---
import BaseLayout from '../../layouts/BaseLayout.astro';
import PostCard from '../../components/PostCard.astro';
import TagBadge from '../../components/TagBadge.astro';
import SearchInput from '../../components/SearchInput.astro';
import { getCollection } from 'astro:content';
import { sortPostsByDate, getUniqueTags, filterPostsByTag } from '../../utils/helpers';

export async function getStaticPaths() {
  const allPosts = await getCollection('blog', ({ data }) => !data.draft);
  const uniqueTags = getUniqueTags(allPosts);
  
  return uniqueTags.map(tag => {
    const filteredPosts = filterPostsByTag(allPosts, tag);
    return {
      params: { tag: tag.toLowerCase().replace(/\s+/g, '-').replace(/\//g, '-') },
      props: { tag, posts: sortPostsByDate(filteredPosts) },
    };
  });
}

const { tag, posts } = Astro.props;
const allPosts = await getCollection('blog', ({ data }) => !data.draft);
const allTags = getUniqueTags(allPosts);
---

<BaseLayout 
  title={`Posts tagged "${tag}"`}
  description={`All blog posts about ${tag}`}
>
  <div class="tag-page">
    <div class="container">
      <div class="wide-wrapper">
        <header class="page-header">
          <div class="tag-title">
            <TagBadge tag={tag} clickable={false} />
            <h1>{posts.length} {posts.length === 1 ? 'Post' : 'Posts'}</h1>
          </div>
          <p class="page-description">
            Exploring {tag.toLowerCase()} and related topics
          </p>
        </header>
        
        <div class="search-section">
          <SearchInput />
        </div>
        
        <div class="filters">
          <div class="filter-section">
            <h3>Filter by Topic</h3>
            <div class="tags-filter">
              <a href="/blog" class="tag-badge">All</a>
              {allTags.map(t => {
                const tagSlug = t.toLowerCase().replace(/\s+/g, '-').replace(/\//g, '-');
                const isActive = t === tag;
                return (
                  <a 
                    href={`/tags/${tagSlug}`} 
                    class={`tag-badge ${isActive ? 'active' : ''}`}
                  >
                    {t}
                  </a>
                );
              })}
            </div>
          </div>
        </div>
        
        <div class="sidebar-layout">
          <div class="main-content">
            <div class="posts-list">
              {posts.map(post => (
                <PostCard
                  title={post.data.title}
                  description={post.data.description}
                  date={post.data.date}
                  tags={post.data.tags}
                  slug={post.slug}
                />
              ))}
            </div>
          </div>
          
          <aside class="sidebar">
            <div class="sidebar-section">
              <h3>All Topics</h3>
              <div class="tags-list">
                {allTags.map(t => (
                  <TagBadge tag={t} />
                ))}
              </div>
            </div>
            
            <div class="sidebar-section">
              <h3>Explore</h3>
              <ul class="explore-links">
                <li><a href="/blog">All Posts</a></li>
                <li><a href="/about">About Me</a></li>
                <li><a href="/contact">Get in Touch</a></li>
              </ul>
            </div>
          </aside>
        </div>
      </div>
    </div>
  </div>
</BaseLayout>

<style>
  .tag-page {
    padding: var(--space-xl) 0 var(--space-2xl);
  }
  
  .page-header {
    text-align: center;
    margin-bottom: var(--space-2xl);
    padding-bottom: var(--space-xl);
    border-bottom: 1px solid var(--color-border);
  }
  
  .tag-title {
    display: flex;
    align-items: center;
    justify-content: center;
    gap: var(--space-md);
    margin-bottom: var(--space-sm);
  }
  
  .tag-title h1 {
    margin: 0;
    font-size: 2rem;
  }
  
  .page-description {
    font-size: 1.25rem;
    color: var(--color-text-secondary);
  }
  
  .search-section {
    margin-bottom: var(--space-2xl);
    padding: 0 var(--space-md);
  }
  
  .filters {
    margin-bottom: var(--space-2xl);
  }
  
  .filter-section h3 {
    font-size: 1rem;
    font-weight: 600;
    text-transform: uppercase;
    letter-spacing: 0.05em;
    color: var(--color-text-secondary);
    margin-bottom: var(--space-md);
  }
  
  .tags-filter {
    display: flex;
    flex-wrap: wrap;
    gap: var(--space-sm);
  }
  
  .tags-filter .tag-badge {
    display: inline-block;
    padding: 0.25rem 0.75rem;
    font-size: 0.8rem;
    font-weight: 500;
    color: var(--color-accent);
    background: var(--color-code-bg);
    border-radius: 4px;
    text-decoration: none;
    transition: all var(--transition-fast);
    border: 1px solid var(--color-border);
  }
  
  .tags-filter .tag-badge:hover {
    background: var(--color-surface);
    border-color: var(--color-accent);
    transform: translateY(-1px);
  }
  
  .tags-filter .tag-badge.active {
    background: var(--color-accent);
    color: white;
    border-color: var(--color-accent);
  }
  
  .tags-filter .tag-badge.active:hover {
    background: var(--color-accent-hover);
    border-color: var(--color-accent-hover);
    transform: translateY(-1px);
  }
  
  .sidebar-layout {
    display: grid;
    grid-template-columns: 2fr 1fr;
    gap: var(--space-xl);
  }
  
  .posts-list {
    display: flex;
    flex-direction: column;
  }
  
  .sidebar {
    position: sticky;
    top: calc(var(--space-xl) + 80px);
    align-self: start;
  }
  
  .sidebar-section {
    padding: var(--space-md);
    background: var(--color-surface);
    border: 1px solid var(--color-border);
    border-radius: 8px;
    margin-bottom: var(--space-md);
  }
  
  .sidebar-section h3 {
    font-size: 1rem;
    font-weight: 600;
    text-transform: uppercase;
    letter-spacing: 0.05em;
    color: var(--color-text-secondary);
    margin-bottom: var(--space-sm);
  }
  
  .tags-list {
    display: flex;
    flex-wrap: wrap;
    gap: var(--space-xs);
  }
  
  .explore-links {
    list-style: none;
    padding: 0;
    margin: 0;
  }
  
  .explore-links li {
    margin-bottom: var(--space-xs);
  }
  
  .explore-links a {
    color: var(--color-text-secondary);
    text-decoration: none;
    transition: color var(--transition-fast);
  }
  
  .explore-links a:hover {
    color: var(--color-accent);
  }
  
  @media (max-width: 1024px) {
    .sidebar-layout {
      grid-template-columns: 1fr;
    }
    
    .sidebar {
      position: static;
    }
    
    .sidebar-section {
      display: inline-block;
      margin-right: var(--space-md);
    }
  }
  
  @media (max-width: 768px) {
    .tag-title {
      flex-direction: column;
      gap: var(--space-sm);
    }
    
    .tag-title h1 {
      font-size: 1.5rem;
    }
  }
</style>
